services:
  - type: web
    name: btc-ai-dashboard
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app_fastapi:app --host 0.0.0.0 --port 10000"
    plan: free
